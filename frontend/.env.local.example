# Base URL of the FastAPI backend.
# Copy this file to frontend/.env.local if you ever need to point the app
# at a different backend. The default below matches the local dev setup.
NEXT_PUBLIC_API_URL=http://localhost:8000
