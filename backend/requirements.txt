# Web framework
fastapi>=0.115

# ASGI server (run with: uvicorn app.main:app --reload)
uvicorn>=0.30

# Loads backend/.env into environment variables (used by app/config.py)
python-dotenv>=1.0

# Parses multipart form uploads (required by FastAPI for UploadFile/File(...))
python-multipart>=0.0.9

# AI runtime - Featherless is OpenAI-compatible, so we use the OpenAI SDK
# (drives the vision agent + text chat via app/services/featherless_service.py)
openai>=1.40

# Agent orchestration - the LangGraph complaint workflow
# (backend/app/workflows/complaint_workflow.py)
langgraph>=0.2

# Supabase - complaint persistence + photo storage
# (app/services/supabase_service.py + app/services/storage_service.py).
# The storage client ships inside this package. Create the table + the
# complaint-photos bucket first: run backend/supabase/schema.sql in the
# dashboard (it is idempotent).
supabase>=2.0

# Location agent - reverse geocoding via OpenStreetMap Nominatim
# (backend/app/agents/location_agent.py)
geopy>=2.4

# Video upload - frame extraction for the vision agent's analyze_video
# (backend/app/agents/vision_agent.py). Headless build = no GUI deps,
# safe on servers/Vercel.
opencv-python-headless>=4.9
